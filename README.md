# homebrew-devtrim

Homebrew tap for [devtrim](https://github.com/mneves75/devtrim) — developer-machine disk hygiene for macOS.

```bash
brew install mneves75/devtrim/devtrim
```

Formulae install the attested release binary from GitHub Releases, verified against the published SHA-256, plus generated shell completions and the man page.
